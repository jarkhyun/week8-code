# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
env:
  REACT_APP_VERSION: ${{ secrets.REACT_APP_VERSION }}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "API key in secrets - ${{ secrets.REACT_APP_VERSION }} API key in env - ${{ env.REACT_APP_VERSION }}"
      - uses: actions/checkout@v2
      - run: cd github-app && npm install && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_GITHUB_APPLICATION_E4A37 }}'
          channelId: live
          projectId: github-application-e4a37
          entryPoint: ./github-app
